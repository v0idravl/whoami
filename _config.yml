title: "v0idravl // whoami"
description: "v0idravl — finds what doesn't belong and proves it: behavioral detection across people, systems, and code. Offline-first, evidence-first, low-resource high-capability."
url: "https://v0idravl.github.io"
baseurl: "/whoami"
author: "v0idravl"

markdown: kramdown
highlighter: rouge

kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: "highlight"

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
  - README.md
